# Archive Snapshot `pre_reorg_v0.7.1`

This archive contains the documentation tree as it existed before the documentation reorganization on top of release `v0.7.1`.

Purpose of this snapshot:

- preserve all historic notes, drafts, images and hardware files
- keep the active `doc/` root focused on maintained documentation
- allow later migration of selected legacy content into the new structure

Archived content is stored under:

- [`legacy_docs/`](legacy_docs/)

No attempt was made to normalize or rewrite the archived files during this move.
